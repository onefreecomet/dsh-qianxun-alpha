# 凭据 — 严禁入库（BRAIN 登录凭据，任何格式都排除）
credential.txt
credential.json
.credential.json
.braintoken
*.env
.env*

# 运行时数据（SQLite / 日志 / 结果）
data/
*.db
*.db-*
__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/

# PnL 缓存（本地 BRAIN 代理下载的 alpha PnL 数据 —— 严禁入库）
pnl/
pnl_cache/

# 日志
*.log

# 个人文件
.DS_Store
Thumbs.db

# 虚拟环境
.venv/
venv/

# Osmosis 运行产物（报告含真实 alpha_id/信号构成 —— 严禁入库；源码除外）
osmosis_runs/
osmosis_report_*.csv

# ARC 回测扩展 —— 从第三方「ARC 回测交付包」移植（见 arc_service.py 头部），
# 仅本地使用，**不对外分发**。本仓库是 public，所以这几个文件永不入库。
# （qianxund.py 里对 ARC 的接线是隔离扩展模式：import 失败只打印告警并禁用，
#  因此缺这几个文件时引擎功能完整、只是没有 /arc 页面。）
arc_service.py
arc_runner.py
arc_ui.html

# 本地改造前快照（*.bak / *.bak_*），永不入库
*.bak
*.bak_*
